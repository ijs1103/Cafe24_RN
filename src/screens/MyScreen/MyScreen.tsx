import { View } from 'react-native';
import { SigninLayout } from './SigninLayout';

export const MyScreen: React.FC = () => {
	return (
		<View style={{ flex: 1 }}>
			<SigninLayout />
		</View>
	);
};
